# Core runtime
python-dotenv>=1.0.1
psycopg[binary]>=3.2.3
qdrant-client>=1.12.0
openai>=1.54.0

# Ingest pipeline
yt-dlp>=2024.11.4
scenedetect[opencv]>=0.6.4
ffmpeg-python>=0.2.0
Pillow>=10.4.0
numpy>=1.26.0

# Open CLIP for frame embeddings
torch>=2.4.0
open-clip-torch>=2.30.0

# HTTP for Vercel Blob upload + Deepgram REST
httpx>=0.27.0

# Audio features (quality + energy) for clip-worthiness scoring
librosa>=0.10.2
soundfile>=0.12.1

# Voice fingerprinting — assign Deepgram speaker clusters to alex vs attendee
# by similarity to a reference Alex audio clip.
Resemblyzer>=0.1.4
